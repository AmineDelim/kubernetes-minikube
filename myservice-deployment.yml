apiVersion: apps/v1
kind: Deployment
metadata:
  name: myservice
spec:
  replicas: 10
  selector:
    matchLabels:
      app: myservice
  template:
    metadata:
      labels:
        app: myservice
    spec:
      containers:
        - image: aminedelim/myservice:v2
          imagePullPolicy: IfNotPresent
          name: myservice
      restartPolicy: Always
